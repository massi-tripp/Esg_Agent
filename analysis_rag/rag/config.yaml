data:
  text_dir: analysis_rag/data/output/text_images

index:
  type: CHROMA
  collection_name: ESG_RAG
  persist_directory: analysis_rag/data/benchmark/chroma  # cartella del DB Chroma

chunking:
  size: 2000
  overlap: 300
  min_chars: 300

retriever:
  k: 8
  mmr: false

metadata_fields: [company_id, doc_id, page, year, lang, section_title]

taxonomy_yaml: configs/taxonomy.yaml

outputs:
  predictions_jsonl: analysis_rag/data/benchmark/predictions.jsonl
